/*WA*/
#include <stdio.h>
#include <string.h>

void solve(void);

int main(void)
{
    solve();
    return 0;
}

void solve(void)
{
    char s[100];
    while (scanf("%[^EOF]s", s)) {
        
            printf("NO\n");
        fflush(stdout);
    }
}
